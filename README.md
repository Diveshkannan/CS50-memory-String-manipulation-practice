# CS50-memory-String-manipulation-practice
Manual string copy and capitalization program built in C as part of CS50 practice. The program copies a string into a separate character array, preserves the original string, and capitalizes the first character using toupper(). Focused on understanding strings, arrays, loops, and memory behavior in C.
